[package]
name = "lib-simulation"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
lib-genetic-algorithm = { path = "../genetic-algorithm" }
lib-neural-network = { path = "../neural-netwok" }
serde = { version = "1.0", features = ["derive"] }
